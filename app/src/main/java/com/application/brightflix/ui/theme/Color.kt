package com.application.brightflix.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * A cinematic palette: near-black surfaces so poster artwork carries the colour, with a
 * single warm amber accent.
 *
 * Amber rather than the Compose template's purple — it reads as cinema and awards, and it
 * holds contrast against film artwork of any hue. Only one accent is used, so a highlighted
 * element always means the same thing.
 */

// Dark — the primary experience.
val AmberPrimary = Color(0xFFE8B33C)
val AmberOnPrimary = Color(0xFF3A2A00)
val AmberContainer = Color(0xFF53400B)
val AmberOnContainer = Color(0xFFFFDEA1)

val CinemaBackground = Color(0xFF0C0C10)
val CinemaSurface = Color(0xFF131318)
val CinemaSurfaceVariant = Color(0xFF1E1E25)
val CinemaOnSurface = Color(0xFFE7E4E0)
val CinemaOnSurfaceVariant = Color(0xFFB6B2AC)
val CinemaOutline = Color(0xFF48454E)

val SlateSecondary = Color(0xFFCFC6B4)
val SlateOnSecondary = Color(0xFF352F1E)
val SlateContainer = Color(0xFF4C4534)
val SlateOnContainer = Color(0xFFEBE2CF)

val ErrorDark = Color(0xFFFFB4AB)
val OnErrorDark = Color(0xFF690005)
val ErrorContainerDark = Color(0xFF93000A)
val OnErrorContainerDark = Color(0xFFFFDAD6)

// Light — a warm paper tone rather than pure white, so it belongs to the same family.
val AmberPrimaryLight = Color(0xFF7A5900)
val OnAmberPrimaryLight = Color(0xFFFFFFFF)
val AmberContainerLight = Color(0xFFFFDEA1)
val OnAmberContainerLight = Color(0xFF261A00)

val PaperBackground = Color(0xFFFFFBF3)
val PaperSurface = Color(0xFFFFFBF3)
val PaperSurfaceVariant = Color(0xFFEDE1CF)
val PaperOnSurface = Color(0xFF1E1B16)
val PaperOnSurfaceVariant = Color(0xFF4D4639)
val PaperOutline = Color(0xFF7F7768)

val SlateSecondaryLight = Color(0xFF655E4B)
val OnSlateSecondaryLight = Color(0xFFFFFFFF)
val SlateContainerLight = Color(0xFFECE2CA)
val OnSlateContainerLight = Color(0xFF201B0D)

val ErrorLight = Color(0xFFBA1A1A)
val OnErrorLight = Color(0xFFFFFFFF)
val ErrorContainerLight = Color(0xFFFFDAD6)
val OnErrorContainerLight = Color(0xFF410002)
