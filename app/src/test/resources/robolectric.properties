# Robolectric runs the Room and Compose tests on the JVM so `./gradlew test` executes them
# for real. Pinned to an SDK level Robolectric ships a runtime for — the project compiles
# against SDK 37, which is newer than Robolectric's supported range.
sdk=34
